spring:
  datasource:
    url: "jdbc:tc:postgresql:14-alpine:///meter-readings"
    username: "meter"
    password: "readings"
  jpa:
    properties:
      hibernate:
        dialect: "org.hibernate.dialect.PostgreSQLDialect"
      show_sql: false